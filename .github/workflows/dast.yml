# ============================================
# WORKFLOW: DAST com OWASP ZAP
# ============================================
name: 🔍 DAST Scan

on:
  # Execução manual
  workflow_dispatch:
  
  # Agendado: Segunda às 2h
  schedule:
    - cron: '0 2 * * 1'

# Permissões mínimas necessárias
permissions:
  contents: read

jobs:
  # ============================================
  # JOB: ZAP Baseline Scan
  # ============================================
  zap-baseline:
    name: 🕷️ OWASP ZAP
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      # Executa ZAP via Docker (evita bug de artifact da action oficial)
      # Nota: ZAP container usa user 'zap' (uid 1000), chmod 766 permite escrita
      - name: 🕷️ ZAP Baseline Scan
        run: |
          mkdir -p reports && chmod 766 reports
          docker run --rm \
            -v ${{ github.workspace }}:/zap/wrk/:ro \
            -v ${{ github.workspace }}/reports:/zap/wrk/reports/:rw \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py \
            -t ${{ secrets.STAGING_URL }} \
            -c /zap/wrk/.zap/rules.tsv \
            -J /zap/wrk/reports/report.json \
            -r /zap/wrk/reports/report.html \
            -a || true

      - name: 📤 Upload Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-reports
          path: reports/
          retention-days: 30
